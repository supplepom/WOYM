<section class="hero is-success">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        Words
      </h1>
    </div>
  </div>
</section>

<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-7">
        <div class="card">
          <div class="card-image">
            <figure class="image is-4by3">
              <%= attachment_image_tag @word, :image %>
            </figure>
          </div>
          <div class="card-content">
            <div class="media">
              <div class="media-content">
                <p class="title is-4"><%= @word.title %></p>
              </div>
            </div>
            <div class="content">
              <table class="table is-narrow">
                <tr>
                  <th>story</th>
                </tr>
                <tr>
                  <td><%= simple_format @word.story %></td>
                </tr>
              </table>
              <% if @word.user.id == current_user.id %>
                <%= link_to "Edit", edit_word_path(@word), class: "button is-success" %>
              <% end %>
            </div>
          </div>
        </div>
      </div>

      <div class="column is-4">
        <article class="panel is-link">
          <p class="panel-heading">
            By <%= @word.user.name %>
          </p>
          <div class="panel-block">
            <p class="control">
              <%= @word.user.profile %>
            </p>
          </div>
          <%= link_to user_path(@word.user), class: "panel-block" do %>
            <span class="panel-icon">
              <i class="fas fa-user" aria-hidden="true"></i>
            </span>
            <%= @word.user.name %> さんのページへ
          <% end %>
        </article>
      </div>
    </div>
  </div>
</section>